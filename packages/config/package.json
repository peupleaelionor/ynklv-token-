{
  "name": "@ynklv/config",
  "version": "0.1.0",
  "description": "Shared configuration for the YNKLV ecosystem: chains, addresses, membership thresholds.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"@ynklv/config: no lint\"",
    "test": "echo \"@ynklv/config: no tests\"",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@ynklv/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
